{
  "name": "demo",
  "version": "1.0.0",
  "description": "electron demo",
  "main": "index.js",
  "scripts": {
    "start": "electron main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-win": "npm run build && build --x64 --win",
    "package-nix": "npm run build && build --x64 --linux"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^15.1.2"
  },
  "dependencies": {
    "electron-builder": "^22.13.1"
  }
}
